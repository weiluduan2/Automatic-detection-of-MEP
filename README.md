# Automatic-detection-of-MEP
Model Weights
The model weights file (best_model.pth) is available in the releases section due to its large size. To use the model:

Download the model file from Releases
Place the downloaded best_model.pth file in the model directory:

project_root/

├── Demo/
  ├── model/
       └── best_model.pth
└── README.md
